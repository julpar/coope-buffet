############################################
# Core service URLs
############################################

# Public URL of the staff frontend (used to build permanent login links/QR)
# Example (local dev): http://localhost:8080
SERVICE_URL_WEB_STAFF=http://localhost:8080

# Base URL where this backend API is publicly reachable (used by MercadoPago webhooks)
# In local dev you can use an ngrok/tunneling URL, e.g. https://abcd-1234.ngrok.io
SERVICE_URL_APP=

# Public URL of the customer frontend (used for MercadoPago return URLs)
# Example (local dev): http://localhost:8080
SERVICE_URL_WEB_CUSTOMER=

# Optional base URL used in some fallbacks when SERVICE_URL_WEB_STAFF is not set
# Example: https://api.example.com
BASE_URL=

############################################
# Server & CORS
############################################

# Port for the Nest server (default 3000)
PORT=3000

# Comma-separated list of allowed CORS origins
# Example: https://staff.example.com,https://customer.example.com
CORS_ORIGINS=

# If set to true, allow any origin (use with caution)
CORS_ALLOW_ALL=false

############################################
# MercadoPago
############################################

# MercadoPago Access Token (test or production)
# Obtain from https://www.mercadopago.com.ar/developers/panel/credentials
MP_ACCESS_TOKEN=

############################################
# Redis
############################################

# Redis connection URL (default redis://localhost:6379)
REDIS_URL=redis://localhost:6379

############################################
# Cloudflare R2 (optional) — used for staff image uploads
############################################

# R2 account and S3 credentials
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# Bucket name to store public assets
R2_BUCKET=

# Optional custom S3 endpoint for R2 (if not set, derived from R2_ACCOUNT_ID)
# Example: https://<account_id>.r2.cloudflarestorage.com
R2_S3_ENDPOINT=

# Public base URL used to serve uploaded files (e.g., a custom domain or CDN)
# Example: https://static.example.com
R2_PUBLIC_BASE=

# Upload prefix under the bucket (no leading/trailing slash). Defaults to "menu".
# Set to empty to upload to the bucket root.
R2_UPLOAD_PREFIX=menu

# Runtime environment (optional)
# Example: development | production
NODE_ENV=development