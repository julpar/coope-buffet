{
  "name": "buffet-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "app-*",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start --parallel"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }
}
